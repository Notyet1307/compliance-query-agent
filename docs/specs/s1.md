# CQA-S1/r0：第一条真实等保知识回答

状态：OWNER_REVIEWED。用户已审阅认可第一版范围、CLI 验收面、AC、模型、公开样例非零留存边界及 10 元／20 次约束，并已授权将 S1 文档提交／推送及三个工单发布到 GitHub；不构成源码实施、服务开通或真实调用授权。来源审核及其他执行闸门仍须满足。

父范围：CQA-MVP；承接 SEED-S1。X1 复核已获用户认可，但 X1 的合成试验权限和证据不扩展至本切片。三个子工单已发布为 GitHub #1、#2、#3，固定引用规格提交 `bf8a731d7ed84c8dc26c8b7b38b7472becf68488`；父范围未创建独立 Issue。本文件不维护第二套任务状态，发布不授予实施或运行权限。

## Problem Statement

安全售前／咨询人员需要针对等保定级、备案问题获得可核对原文的参考草稿。当前合成样例不能证明真实资料、真实模型与人工参考答案形成闭环；地方窗口口径、旧版本及模型记忆容易被误当全国现行义务。

## Solution

以少量经人工确认的官方公开资料构成固定本地快照，回答全国通用依据层面的问题，使用一个固定境内模型生成有引用的中文参考草稿。依据不足或时效、版本有疑问时明确拒绝形成实质结论。资料取得、参考答案核验与模型生成分别留证，不把引用 ID 校验当语义证明。

地方入口、地方补充材料及窗口口径不进入首版全国答案；实际企业等级认定、是否合规通过和专业测评结论不由本服务作出。保持另外四个领域的范围声明，不以等保样例宣称其他四域已具备真实知识覆盖。

## User Stories

1. 作为安全售前／咨询人员，我希望按明确查询日期提出全国等保问题，以便知道回答采用的时间边界。
2. 作为查询者，我希望区分不同备案情形的条件、起算点和材料要求，以免把不同主体或阶段混为一谈。
3. 作为查询者，我希望逐项核对原文、定位、文档版本和来源，以便自行复核参考草稿。
4. 作为查询者，我希望没有依据时明确得到依据不足，而不是模型凭记忆补写条文。
5. 作为查询者，我希望时效未经核验或版本冲突时得到待复核结果，而不是看起来确定的现行结论。
6. 作为查询者，我希望地方办事指南不会被泛化成全国统一要求。
7. 作为资料审核者，我希望来源许可、效力核验与参考答案分别记录，以免一次网页抓取被当作全部审核通过。
8. 作为验收者，我希望对条件、否定、主体、数值和引用含义进行人工核验，不由模型自行打分通过。
9. 作为操作者，我希望只发送批准的公开样例，明确服务地域、留存风险、模型和费用边界。
10. 作为操作者，我希望失败、超时和结果未知不会触发自动重试或重新获得试验额度。
11. 作为验收者，我希望真实调用与本地模拟证据分开，能复核具体代码、配置、资料、输入和输出快照。

## Implementation Decisions

### 输入、资料与回答

- 复用既有查询协议、CLI、业务引擎、资料资格过滤、生成客户端与回执；不新增聊天平台、上传 API、工具选择或通用任务控制器。
- 首版请求使用 `topic=mlps`、`jurisdiction=CN`，不针对特定行业的全国问题使用 `industry=all`，查询日期显式固定；关联字段仍只是绑定，不授予权限。
- 本地资料必须来自逐项审核的真实公开来源，正常样例不得使用 synthetic 或把演示资料改名。每个来源固定原文、定位、版本、公开时间、有效区间、核验记录、文本与快照摘要；效力、适用条件与使用许可保留可复核记录。仅补充样例确实需要的元数据，不一次性实现治理建议的全部字段。
- 官网更新时间、代理抓取时间、搜索摘要和当前仍能访问，都不是现行效力的充分证明。来源不满足资格时保留 `NEEDS_REVIEW`，不为了取得正常样例而伪填审核日期或更换 status。
- 复用当前关键词检索，只有冻结真实样例证明召回不足时才作有针对性的修改；不得针对某条测试问题硬编码答案。
- 每条 claim 对应本次证据集合内的 ID；引用的 URL、原文、版本与摘要由程序产生，不接受模型覆盖。始终保留 `humanReviewRequired:true`、`entailmentVerified:false`。
- 继续使用 `DRAFT_READY`、`INSUFFICIENT_EVIDENCE`、`NEEDS_REVIEW`；`REFERENCE_ONLY` 不算真实 LLM 正常样例通过。

### 模型与数据边界（用户已确认）

- 百炼华北2（北京）、阿里云直供 `qwen3.7-plus-2026-05-26`，按量付费 API；非思考、非流式、JSON Object 输出。实际业务空间 API Host 与模型推理部署范围由操作者核实固定；不换别名、第三方直供或全球推理。
- 外发范围仅固定提示词、经确认的非敏感样例问题、查询日期、证据 ID／标题／定位／公开原文片段。不发送客户资料、真实系统详情、内部制度、完整业务回执或无关关联数据。
- 用户接受本轮公开样例的非零留存边界：合同未给统一留存天数，备份可能不能立即删除。不扩展到客户数据，不主动开启额外推理日志、训练、数据回流或联网搜索；不代用户签约。
- 复用现有 HTTP 客户端的固定端点、无重定向、无环境代理和无自动重试约束。密钥仅经约定环境引用注入，不入源码、公开证据或对话。
- 必要兼容变更须明确关闭思考并正确限制完整输出；不能把既有仅限制回答的 `max_tokens` 说成完整计费输出上限。使用支持的完整输出限制参数，保留本地结构与引用校验；不记录隐藏推理或完整 provider 原始错误。

### 一次性费用与失败边界

- 总预算 10 元、最多 20 次真实请求。已发请求即计数，包括失败、超时和结果未知；不会因新 requestId、重启或清理本地回执重新获得额度。仅批准的一次性样例运行适用，不宣称全系统生产配额隔离。
- 在发送前检查额度并持久化保守占用；存储失败或占用结果不明不发送。结果未知不退款额度、不自动重试；同请求重放沿用现有回执语义，不增加真实调用。
- 冻结完整输入上限与费用推导，目标每次完整输入不超过 8,000 Token、完整输出限制 2,048 Token。输入计数包含提示词及所有外发字段；必须使用可验证的计数或有证据支持的保守上界，不能用字符数代替 Token 数。价格与输出误差按执行前官方资料核验，不能依赖优惠或免费额度证明预算。
- 每次发送前，已占用费用加本次保守费用必须不超过预算；对账只记录必要用量、费用和请求标识，不留隐藏推理。账单延迟不构成继续发送的理由。无法建立可靠费用上界或次数控制则 BLOCKED，不运行。
- 控制仅为此切片必要的有限调用与证据保全，不引入通用计费平台、队列或重试框架。

## Testing Decisions

主验收面建议复用现有 `query` CLI：输入固定请求和配置，观察最终结果与保存回执，并对照真实服务的必要调用／用量证据。局部异常复用现有业务引擎与本地 HTTP 协议测试；不另建测试接口。该验收面随本文一起由用户审阅。

| AC | 输入／情形 | 可观察通过条件 |
|---|---|---|
| S1-A01 | 至少一个已审核真实全国等保问题 | 实际模型生成 `DRAFT_READY`；人工按冻结参考答案核对主体、条件、起算点、期限／材料及每条引用含义，全部满足才通过；不能用语句格式或模型自评代替 |
| S1-A02 | 快照中无可用依据 | `INSUFFICIENT_EVIDENCE`，无虚构义务；本地已确定无依据时真实模型调用增量为零 |
| S1-A03 | 相关来源时效未知、核验不足或未审核 | `NEEDS_REVIEW`，不发送模型请求、不冒充现行依据 |
| S1-A04 | 版本有效区间边界及重叠冲突 | 结束日不继续选旧版；区间内历史查询按记录选版本；冲突返回待复核，不挑选最方便的版本 |
| S1-A05 | 受控恶意正文 | 来源文字不能改端点、模型、权限或执行命令；不能诱发伪造引用、无来源义务或泄密；真实模型输出仍须人工核验，不能仅凭提示词或一次样例声称通用注入防护成立 |
| S1-A06 | 公开资料、范围与权限 | 原文／定位／版本／摘要可复核，许可和核验记录齐全；地方口径不泛化；其他四域不被标真实验收通过 |
| S1-A07 | 外发字段与模型输出限制 | 仅发送已批准字段，固定北京服务及模型；明确关闭思考、完整输出限制有效；不接受工具调用、伪造引用或无效输出，不记录秘密和隐藏推理 |
| S1-A08 | 次数／费用将超限，发送前存储失败，失败／未知后再提交 | 超限或无法可靠预留时零新增发送；已发失败／未知保守占额度，不自动重试、不通过重启刷新额度；总次数和费用在授权内 |
| S1-A09 | 相同请求重放与不同输入复用 ID | 完成结果原样重放且零新增模型调用；不同输入冲突；未完成请求不自动重试 |
| S1-A10 | 候选固定与证据交接 | 每项 AC 有预期／实际、真实命令、环境、退出结果、代码／配置／资料／输入摘要及人工复核结论；mock、live、FAIL、BLOCKED、NOT_RUN 分开 |

原始 SEED-S1 的正常、无依据、时效不明、版本边界、恶意正文五类链路均保留。安全／版本对抗数据必须明确标注为受控测试变体，与真实法规快照分离；不得把伪造效力、日期或恶意附文标成真实法规已审核内容。是否向真实模型发送受控变体须进入最终样例清单并经批准。

先例：现有五域与异常、无依据不调用模型、历史有效区间、来源摘要篡改、引用伪造、未完成不重试、网络与重定向、回执幂等测试，以及 CLI／loopback smoke。仅为新增真实不确定边界保留回归测试，不断言源码文本、转发细节或固定回答措辞。

实施后使用仓库 `make verify`，目标 Go 读取 `.go-version`；真实模型验收另行运行，离线 PASS 不代替 live。金额／次数控制先用受控本地服务证实拒绝边界，不为测试超额而实际花费超出授权。

## Out of Scope

实际企业等级认定或合规通过；专业测评结论；标准全文抓取／再分发；客户数据；地方专属办事流程；另外四域真实知识覆盖；向量数据库／队列／平台重建；模型训练、多模型投票或自动语义认证；放宽 X1 限制；恢复 X1 资源；正式原生真实知识部署（S2）；Accord 写操作和受管调用集成（S3）；公开部署、Issue 发布、commit／push／merge／release。

## Further Notes

### 执行前闸门

1. 用户已审阅认可本文验收面与 AC；继续编制自包含候选工单，工单粒度与实施授权分别确认，不以标签获得权限。
2. 固定实际问法、查询日期、逐条来源资格／许可、人工参考答案及对抗样例清单。用户确认参考答案；专业能力不足处标待复核，不能由助手代签。
3. 实施获单独授权后，形成通过离线验证的候选、可验证完整输入／输出和费用上界、次数占用与失败处置方案。
4. 操作者核实账号、业务空间、境内推理范围、模型可用性和适用条款，按约定注入密钥；不得要求在对话中粘贴密钥。
5. 固定代码／资料／配置／样例／执行命令与证据输出范围，再取得一次性真实运行授权。任何闸门未满足，真实调用保持 NOT_RUN／BLOCKED。

### 当前证据与下一验收入口

- 产品与原始 AC：[MVP](mvp.md)、[SEED-S1](../issue-seeds/S1.md)。
- 模型官方事实、合同条款与条件费用算式：[模型核验记录](../research/s1-model-candidate.md)。
- 官方资料及取得限制：[来源候选记录](../research/s1-source-candidates.md)。官方直读失败，代理转录不可冒充已认证原始快照；2007办法的2026现行效力未终审，正常真实样例在完成来源审核前 BLOCKED。
- 来源与人工答案治理：[知识治理](../knowledge-governance.md)。
- 当前只完成决策和文档；S1-A01 至 S1-A10 的新切片执行均为 NOT_RUN。来源现行性、使用许可与参考答案尚未终审，不存在可直接运行的已批准真实 corpus。
- 候选工单粒度与依赖已获用户确认；下一入口为逐项实施授权。来源核验结果用于随后冻结参考答案，不静默替换为更容易通过的历史问法；真实运行仍须单独授权。
- 文档验证：在本机 darwin/arm64 执行 `python3 scripts/repo-check.py`，PASS，解析19个 JSON、检查31个本地 Markdown 链接，断链0；上游解析验证 NOT_RUN。本轮没有行为变更，未重跑 `make verify`，未运行真实模型或修改 X1 冻结证据。

### 后续演进意向（不在本版）

用户提出后期可能通过 OctoBus 对接 RAG 增强查询，并提供自主材料上传能力。此为演进意向，不是已批准的上传功能或生产设计。建议复用知识查询边界接入 RAG；上传、解析、审核、版本和访问权限由后端知识服务负责，查询智能体仍使用只读能力。上传身份与范围、文件安全、存储／删除、来源许可、客户保密及哪些内容可进入检索，届时单独收敛规格；上传完成不自动等于来源审核通过。本版不预建上传 API、向量库或权限框架，不扩大 S2 既有 AC。
