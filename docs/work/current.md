# 当前工作入口

当前动作：接管核验本启动包，不直接推进整个产品。

读取 `AGENTS.md`、`docs/handoff.md`，运行批准的本地检查，输出真实接管回执。正式任务状态以用户建立仓库后的 GitHub 为准，本文件不维护第二份状态表。

下一入口是 SEED-B0。未发布 Issue，不知道真实远端，不默认 ready，不继承任何旧 Planner/Controller 状态。

会话结束更新需要继续阅读的材料、真实证据与待确认问题；不写未经证实的“全部已完成”。
