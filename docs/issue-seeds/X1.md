# 验证一次受限运行的真实输入输出与 OctoBus 边界

Seed-ID: SEED-X1

Status: CANDIDATE — 未发布、未批准执行。

Parent: CQA 首个可受管使用的安全合规查询智能体里程碑（正式父 Issue 未建立）。

## User outcome

回答唯一问题：所选 agent-compose 版本能否完成一次只读 OctoBus 查询并返回可绑定结构化结果，同时上游凭据不进入 guest。

## What to build

在获准的合成环境，固定 daemon/guest/OctoBus 版本与镜像；验证原生 capset 注入调用路径、固定命令输入文件与结果通道、日志位置/内容、运行引用和跨沙箱回执边界。只做一个检索方法，不开发通用 Runtime。

## Acceptance criteria

记录真实请求/运行/沙箱引用和最终 JSON；未授权 capset 被拒；检查凭据反射和日志；无法完成时给具体根因及直接 Connect 受限备选的代价；形成可复核接缝 Spec/ADR 草稿。

## Blocked by

SEED-B0

## Out of scope

客户数据、多种驱动对比、新权限平台、多租户、Accord 核心改动。

## Evidence

固定实际代码 SHA/差异、批准 Spec、输入/预期/实际输出和环境。记录真实命令及日志，mock 和 live 分开。不得提交秘密/客户原文，缺环境标 BLOCKED，未执行标 NOT_RUN。

## Publication mapping

未发布；用户批准且核验真实 Git remote 后，由 OMP 按 Seed-ID 去重发布。
