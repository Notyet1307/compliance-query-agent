# 用 agent-compose 承载并通过 OctoBus 完成同一查询

Seed-ID: SEED-S2

Status: CANDIDATE — 未发布、未批准执行。

Parent: CQA 首个可受管使用的安全合规查询智能体里程碑（正式父 Issue 未建立）。

当前收敛：用户已认可单文档受管模型测试与限定同机重放的 [CQA-S2/r0](../specs/s2.md)，并同意继续编写本地候选工单。下列原始真实资料目标和 AC 保留为历史候选，不因测试范围通过而记 PASS；正式模式仍不得载入 synthetic。本 seed 未发布，也不授予实施／运行权限。

## 本轮候选拆分

用户已认可两张纵向切片及 SEED-S2-01 → SEED-S2-02 依赖；模型接入、稳定身份与持久重放在第二张同一候选构建内完成，避免先真实生成、再改变构建／摘要而追加模型调用。每张票自行完成必要正常／异常和真实接缝验证，不另设只有“整理证据”的尾票，不把验证推至 R0。

| 候选 | 未完成的前置候选 | 独立交付 |
|---|---|---|
| [SEED-S2-01：仅查询权限的受管单文档检索](S2-01.md) | 无；实施／资源运行仍须授权 | 真实 guest → 原生 OctoBus Search → 单文档原文结果，G2 认证／目录兼容及权限拒绝；零模型调用 |
| [SEED-S2-02：受管模型草稿与同机重建重放](S2-02.md) | SEED-S2-01 | 同一固定构建完成 DRAFT_READY、同请求／实际容器替换／新 sandbox 重放，G1／G3 及完整 S2 证据映射 |

唯一新增工单阻塞边为 **SEED-S2-01 → SEED-S2-02**：第二张需要第一张已验证的最小权限查询通路、同文档服务包与调用观察办法。S1 已接受测试资料和 X1 既有实现为参考依赖，不新增虚假的待办阻塞票；外部授权与运行条件在每张票内独立列出。

### 规格覆盖归属

这是验收责任映射，不是执行状态或另一份规格；全部验收尚未执行。每张票仍须提供其改动范围的完整证据，最终候选重验受影响的前置边界。

| 规格条目 | 主要归属 | 分段证明与最终核对 |
|---|---|---|
| S2-G1、S2-A01、S2-A04、S2-A12 | SEED-S2-02 | 真实模型代理、草稿及本地受控输出／用量异常 |
| S2-G2、S2-A05 | SEED-S2-01 | 第二张在最终组合候选复核查询权限，不无条件继承旧版本 PASS |
| S2-G3、S2-A07、S2-A08 | SEED-S2-02 | 实际容器替换及新 sandbox 均读原持久回执 |
| S2-A02、S2-A03 | SEED-S2-02 | 第一张交付数据筛选；第二张必须在 LLM 已配置时证明零生成发送 |
| S2-A06、S2-A09、S2-A10、S2-A14 | SEED-S2-02 | 完成重放、身份冲突、存储阻断与并发单执行者 |
| S2-A11、S2-A13 | SEED-S2-02 | 第一张先证明 Search 失败／权限及数据通路；第二张覆盖模型／恢复及完整安全边界 |
| S2-A15 | SEED-S2-01、SEED-S2-02 | 各票独立固定候选／验证／停止；第二张归并完整 S2 证据，不能替前票补未做的真实验证 |

粒度及依赖已获认可；发布、实施、运行与 Git 操作仍各自核对授权，不自动创建 GitHub Issue 或贴执行标签。以下原始种子保留，不把其中真实资料 AC 改成已通过。

## User outcome

相同业务样例在真实受限 guest 中运行，经过 OctoBus 查询授权知识系统并返回结构化候选结果。

## What to build

依据 X1 选择一种正式通路，避免重复 Direct/原生挂载；实现实际知识服务/已有系统适配；使用只读 capset；打通输入 artifact、稳定调用引用、结果读取、持久化/日志策略与取消/未知处理。

## Acceptance criteria

真实 daemon/guest/OctoBus/model 各有版本与凭据边界记录；成功返回可核验结果；错误权限、资料缺失、服务失败、重复提交、guest 重建和取消未知不伪造成功；真实受限模式不能载入 synthetic。

## Blocked by

SEED-S1

## Out of scope

OctoBus admin 权限、自由 shell、任意 URL 查询、生产/公网部署、批量运行多个 Agent。

## Evidence

固定实际代码 SHA/差异、批准 Spec、输入/预期/实际输出和环境。记录真实命令及日志，mock 和 live 分开。不得提交秘密/客户原文，缺环境标 BLOCKED，未执行标 NOT_RUN。

## Publication mapping

未发布；用户批准且核验真实 Git remote 后，由 OMP 按 Seed-ID 去重发布。
