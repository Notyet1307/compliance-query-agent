# 先看这一页

你现在拿到的是一个可以运行和交接的起点，不是“只有目录”，也不是已经把三个平台联通的成品。

## 1. 先看效果

在仓库根目录执行 `make verify`，再执行 `make demo`。第一条正常示例会返回虚构资料的原文、来源定位、版本、日期和 SHA256。`examples/insufficient.json` 的真实业务问法没有匹配知识，所以会明确返回依据不足，而不是编造答案。

默认不开网络、不用真实模型、不读取客户系统。CLI 是当前演示入口；未来业务界面由 Accord 提供，本仓库不另建聊天前端。

## 2. 再确认边界

已确认：五个查询领域，独立智能体，agent-compose 承载，LLM 接口，OctoBus 外部数据，Accord 角色使用。

本启动包的推荐：先做可追溯资料查询，暂不做自动合规裁决；Go 单进程和文件回执；第一条真实样例先选等保；行业样例用运营商，但不把“行业知识”限定成仅运营商。

用户已建立公开仓库并接受 S1 单文档结果和当前 S2 真实查询／测试草稿；本次 Git／GitHub 发布与合并已获授权。跨容器／新 sandbox 重放延期，整 S2 未完成；见 [当前验收摘要](evidence/s2-query/accepted-summary.json)。许可证、真实法规资料资格、客户数据传输、生产及 Accord 接入未因此获批；新的模型调用、费用和资源操作仍须单独授权。

## 3. 保留现有仓库

本仓库已建立 Git 和远端，不要重复初始化或重新创建。提交前核对 `.gitignore` 和文件清单；不要上传 `.env`、`.local/`、个人凭据、客户样本或本机 OMP 会话。

本次仅发布已验收候选、脱敏结果与工单映射；不默认执行 `git add .`，不上传私有原始证据。当前查询切片已获用户接受；持久化部署可靠性与后续运行保持延期／待授权，不从本次 commit、push 或 merge 推导新的执行权限。

## 4. 首次接管指引（已完成，供历史参考）

从根目录启动你的 OMP，先检查 `omp --version`、`omp --help`，再按照已安装版本支持的审批方式进入受控会话。操作手册给出的 `omp --approval-mode always-ask` 由 OMP 首次核验是否适用于本机。

首次接管使用 `prompts/01_OMP接管合规查询智能体.prompt.md`；历史核验结果见 `docs/handoff-receipt.md`。当前交接先读 [S2 当前范围与结果](docs/specs/s2.md#当前优先级修订先验收真实查询与草稿质量)，不重做接管、重放已停止试验或直接“实现整个愿景”。Matt Skills 的配置文件仍不等于安装／依赖成功证明。

## 5. 下一步顺序

B0 接管与环境基线 → X1 受限运行技术试验 → S1 第一条真实知识回答 → S2 agent-compose + OctoBus 交付闭环 → S3 Accord 受管使用 → R0 独立验收。

先认可业务例子和关键方案，再批准发布候选工单；正式工单发布不等于开发授权。每次只推进获准的一条业务链路或小批次。

**交接入口：`docs/handoff.md`；完整设计：`docs/architecture.md`；未解决问题：`docs/decisions-and-assumptions.md`。**
